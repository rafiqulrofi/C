#include<stdio.h>
#include<conio.h>

int main()
    {
       int a,b;
       scanf("%d",&a);
       scanf("%d",&b);
       int add=a+b;
       int sub=a-b;
       int mul=a*b;
       int div=a/b;
       printf("addtion=%d\nsubtraction=%d\nmultiplication=%d\ndivision=%d",add,sub,mul,div);
       
       getch();
       
       return 0;
       }
